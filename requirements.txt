python-nomad==2.0.0
rich>=13.5.2
typer>=0.7.0
pandas>=2.3.3, < 3.0
# 2.3.3 is the last version that supports 3.10, which is the python version running in the papi instance
# TODO: update pandas to 3.* ?
requests >= 2.25.1, < 3.0
